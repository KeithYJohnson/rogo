Above TB Divs
/ For AJAX, setting div class to poll.id
%div{:id => 'ajaxurl', :class => @poll.id}
  :javascript
    pollServer();

.survey-data.span5
  %h1= @poll.title 
  %p= @poll.description

  / My Twilio Number.  
  / User will be able to purchase their own and 
  / It will render here
  %h2 Text to 347-321-7539

  /Will have to change with multi-question feature.
  %h3= @question.title

  %table.table.striped.table-hover
    - @answers.each do |answer|
      %tr
        %td= answer.title
        %td= "TEXT #{answer.id} to Vote"
  %br

.graph.span6
  / Found in root view.  
  %script 
    = render 'survey_graph.js'

.nav-links.span12 
  =link_to "Dashboard", root_path, :class => 'btn btn-inverse'
  =link_to "Edit", edit_poll_path(@poll), :class => 'btn btn-inverse'